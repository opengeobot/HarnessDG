schemaVersion: harnessdg.evidence/v1
evidenceId: EVD-P1014-001
status: DRAFT

task:
  taskId: TASK-P1-014
  requirements: [REQ-AST-001]
  scenarios: [AC-P1-AST-014, AC-P2-EXIT-001, AC-P3-EXIT-001]

governance:
  specValidation:
    command: pwsh ./docs/ai-spec/tools/validate-spec.ps1
    result: PASS
  taskValidation:
    command: pwsh ./docs/ai-spec/tools/validate-task-card.ps1 -TaskPath docs/ai-spec/tasks/TASK-P1-014.md
    result: PASS
  changedPathValidation:
    command: pwsh ./docs/ai-spec/tools/validate-task-card.ps1 -TaskPath docs/ai-spec/tasks/TASK-P1-014.md -CheckChangedPaths
    result: PASS
  completionValidation:
    command: pwsh ./docs/ai-spec/tools/validate-task-card.ps1 -TaskPath docs/ai-spec/tasks/TASK-P1-014.md -CheckCompletion
    result: FAIL

source:
  branch: ai
  commitSha: pending-commit
  dirtyWorktree: false
  dirtyPaths: []

environment:
  observedAt: 2026-07-12T06:10:00Z
  os: linux 6.17.0-35-generic
  timezone: UTC+8
  java: 21
  node: local
  pnpm: local
  docker: available
  composeProfiles: [default]
  externalDependencyVersions: {}

execution:
  workingDirectory: deploy/compose
  command: docker compose config --quiet && bash scripts/verify.sh && bash scripts/verify-journey.sh
  startedAt: 2026-07-12T05:25:00Z
  finishedAt: 2026-07-12T06:10:00Z
  exitCode: 1
  result: PARTIAL
  evidenceLevel: E4

coverage:
  proven:
    - scenarioId: AC-P1-AST-014
      assertion: verify.sh V05 JWT 生命周期 PASS（新增 reset_admin_password 兜底：双密码失败时经 worker python crypt 生成 bcrypt、归一化 $2a$ 前缀、stdin 传入 psql UPDATE smallint must_change_password=0，解决持久化 DB 口令漂移）；V20 对账 Worker PASS（依赖 V05 token）
  notProven:
    - scenarioId: AC-P1-AST-014
      reason: V06/V07/V09/V10/V11/V21 FAIL——admin token 访问 system 端点返回 200 非 403。系预先存在的授权模型冲突（Wave A EffectiveScopeResolver 合并角色绑定 scopes 使管理员菜单可见，致 admin 持有 system:* scopes），V05 修复前因 ACCESS_TOKEN 为空这些断言仅跑 anon=401 而「假 PASS」；属授权模型问题，超出 Wave W（脚本/Compose/E2E）allowedPaths，列为 follow-up
    - scenarioId: AC-P2-EXIT-001
      reason: verify-journey 资产创建旅程 SKIP（INFRA:500，ASSET_PROVISION job enqueue unavailable）；running 后端为 16h 前旧镜像，不含 Wave T/U 改动；PROVISIONED→COMPLETED 轮询修复正确但因 500 未被 exercised。后端重建超时（Maven dependency:go-offline 下载慢，>12min 未完成）→ E4 of rebuilt images notProven
    - scenarioId: AC-P3-EXIT-001
      reason: 同 AC-P2-EXIT-001，发布旅程依赖资产创建，SKIP；E2E Playwright 未执行（需 frontend 重建）

artifacts:
  reports:
    - docker compose config --quiet → PASS
    - verify.sh → V05 PASS（口令重置兜底）、V20 PASS；V06/V07/V09/V10/V11/V21 FAIL（admin authz broad-scopes，预存）；其余 V01-V28 PASS
    - verify-journey.sh → PASS=8 SKIP=10 FAIL=0
    - ./mvnw -o test → 681 unit PASS（Wave W 无后端代码改动）
    - pnpm typecheck/lint → PASS（Wave W 无前端代码改动）
  logs: []
  screenshots: []
  traces: []
  metrics: []
  databaseQueries:
    - reset admin: update iam_user set password_hash='<bcrypt $2a$>', must_change_password=0 where username='admin'

secrets:
  redactionChecked: true
  findings:
    - verify.sh reset_admin_password 仅写入 bcrypt 哈希（不可逆），不输出明文口令；JOURNEY_ADMIN_PASS 不进入日志

limitations:
  skippedTests:
    - 96 Testcontainers IT（disabledWithoutDocker from JVM）
    - E2E Playwright（frontend 重建未完成）
  assumptions:
    - V05 口令重置兜底用 worker 容器 python crypt 生成 bcrypt（$2b$→$2a$ 归一化），stdin 传 psql 避免哈希中 $ 被 sh -c 二次展开，must_change_password 为 smallint 用 0
    - verify-journey PROVISIONED→COMPLETED：ProvisioningStatus 终态为 COMPLETED（AssetSearchDao a.provisioning_status='COMPLETED'），原脚本轮询 PROVISIONED 永不命中致超时 SKIP
    - V06+ admin authz 冲突非 Wave W 引入；Wave T/U 后端改动或可修复资产创建 500，但需重建验证
  environmentDifferences:
    - 后端重建（docker compose build backend worker）超时未完成（Maven 依赖下载慢）；E4 of rebuilt images notProven，留后续会话

review:
  recordedBy: AI (implementation session 2026-07-12)
  reviewedBy: null
  reviewedAt: null
  accepted: false
