# 待确认问题与决策登记

> 状态：`CLOSED`
> 规则：建议默认值只用于帮助决策，在用户确认前不构成需求。
> 所有问题已通过 DEC-010/011/012/013 闭合并 ACCEPTED。

## 1. 第一轮：范围与实施策略

| ID | 问题 | 建议默认值 | 为什么必须确认 | 状态 |
| --- | --- | --- | --- | --- |
| `Q-001` | 规格包覆盖完整 P0-P5，还是先完整规格化当前阶段？ | 建立全局框架，先完成 P0-B 真实性审计和 P1 资产目录 | 决定文档深度、任务切片和验收成本 | `ACCEPTED` → `DEC-001` |
| `Q-002` | 保留现有实现逐项整改，还是允许推倒重做？ | 保留并以证据审计决定局部替换 | 决定迁移、兼容和历史数据策略 | `ACCEPTED` → `DEC-002` |
| `Q-003` | 是否接受 2026-07-02 快照所称"P0-B 已闭环"？ | 暂不接受，逐条重验 P0-B 出口 | 当前规范、契约和代码证据互相矛盾 | `ACCEPTED` → `DEC-003` |
| `Q-004` | 目标 AI IDE 是 Trae 专用还是多 IDE？ | 通用规格核心 + Trae 适配层 | 决定任务格式和 IDE 元数据位置 | `ACCEPTED` → `DEC-006` |
| `Q-005` | 规格评审完成前是否冻结产品代码？ | 冻结 P1+，只允许规格和证据审计 | 防止需求尚未稳定时继续扩大偏差 | `ACCEPTED` → `DEC-007` |
| `Q-006` | 首批必须跑通的用户旅程及优先级是什么？ | 登录/治理 → 数据集发现/详情 → 上传发布下载 → AI 搜索下载与受限贡献 | 用户旅程是纵向任务和 E2E 的边界 | `DEC-005` 已替代 → `DEC-008` |
| `Q-007` | "完成 P0"的精确边界是否严格采用 ADR-0002 的 P0-A/P0-B？ | 是；不改阶段定义，先让 P0-B 真实退出，再依次实现 | 用户确认 P0 未完成，需明确是顺序目标还是改写 P0 | `ACCEPTED` → `DEC-010` |

## 2. 第二轮：组织、主体与权限

| ID | 问题 | 建议默认值 | 影响 | 状态 |
| --- | --- | --- | --- | --- |
| `Q-101` | 是否为单公司租户、公司内多个组织和项目？ | 单租户、多组织、多项目 | 数据隔离、唯一约束和管理权限 | `ACCEPTED` → `DEC-004` |
| `Q-102` | 用户能否同时属于多个组织和项目？ | 可以 | PrincipalContext、列表过滤和角色绑定 | `ACCEPTED` → `DEC-011` |
| `Q-103` | Team 是否是正式领域资源？ | 是；补齐 Team 表、API、成员和权限 | PRD 要求 Owner 可引用 Team，但当前模型缺失 | `ACCEPTED` → `DEC-011` |
| `Q-104` | 资产 Owner 可以是个人、Team，还是必须为 Team？ | 至少一个 Team，可附加个人维护者 | 责任归属、离职交接和授权 | `ACCEPTED` → `DEC-011` |
| `Q-105` | 谁可以创建组织、项目、Team 和资产？ | 平台管理员建组织；组织管理员建项目/Team；项目成员按角色建资产 | 权限矩阵和页面入口 | `ACCEPTED` → `DEC-011` |
| `Q-106` | `PUBLIC` 是否仍只对已认证主体公开？ | 是 | 防匿名访问和搜索枚举 | `ACCEPTED` → `DEC-011` |

## 3. 第三轮：资产、版本和发布

| ID | 问题 | 建议默认值 | 影响 | 状态 |
| --- | --- | --- | --- | --- |
| `Q-201` | 首个可交付版本必须支持 MODEL 和 DATASET 两类吗？ | 两类都支持，共用主流程、分别定义字段 | 表单、验证、卡片和测试组合 | `ACCEPTED` → `DEC-012` |
| `Q-202` | 资产名称、Namespace、重命名和旧别名的精确规则？ | 组织内 Namespace 唯一；重命名保留永久跳转别名 | URL、Gitea 仓库和唯一约束 | `ACCEPTED` → `DEC-012` |
| `Q-203` | 谁可以提交、审批和发布？提交人与审批人能否相同？ | 提交人与审批人分离；平台管理员仅紧急越权并审计 | 四眼原则和状态机 | `ACCEPTED` → `DEC-012` |
| `Q-204` | 哪些 License、敏感等级或文件类型强制审批？ | 所有发布均审批；高敏感/高风险文件追加安全审核 | 策略、通知和审计 | `ACCEPTED` → `DEC-012` |
| `Q-205` | 版本号是否强制 SemVer？数据集日期版本是否允许？ | 模型 SemVer；数据集允许受控日期版本 | 校验、排序和 API Schema | `ACCEPTED` → `DEC-012` |
| `Q-206` | 软删除、归档和对象回收保留期分别多久？ | 元数据 90 天可恢复，正式对象需引用检查后回收 | 生命周期任务和恢复 | `ACCEPTED` → `DEC-012` |

## 4. 第四轮：UI 与非功能目标

| ID | 问题 | 建议默认值 | 影响 | 状态 |
| --- | --- | --- | --- | --- |
| `Q-301` | 第一版是否必须同时支持 zh-CN/en-US 运行时切换？ | 是 | i18n 架构、文案和 E2E | `ACCEPTED` → `DEC-013` |
| `Q-302` | 是否有现成 UI 品牌规范或参考产品？ | 若没有，以 Ant Design 企业后台为基线 | 页面规格和视觉验收 | `ACCEPTED` → `DEC-013` |
| `Q-303` | MVP 用户数、并发、资产数、单文件和单会话规模？ | 暂沿用 PRD：10 万资产、Web 20 GiB 上限 | 性能、索引和容量验收 | `ACCEPTED` → `DEC-013` |
| `Q-304` | Compose 是唯一验收环境还是还需定义生产部署？ | Compose 为功能验收；生产部署另立规格和 ADR | Secret、HA、备份和 SLO | `ACCEPTED` → `DEC-013` |
| `Q-305` | 浏览器 E2E 采用什么工具，是否要求截图/视觉回归？ | Playwright + 关键页面截图 | 页面"完成"的证据标准 | `ACCEPTED` → `DEC-013` |

## 5. 决策记录格式

用户回答后，将问题转换为不可变决策记录：

```yaml
decisionId: DEC-...
questionId: Q-...
status: ACCEPTED
decision: ...
rationale: ...
decidedBy: ...
decidedAt: ...
affectedRequirements: [...]
affectedContracts: [...]
supersedes: null
```
# 待确认问题与决策登记

> 状态：`OPEN`
> 规则：建议默认值只用于帮助决策，在用户确认前不构成需求。

## 1. 第一轮：范围与实施策略

| ID | 问题 | 建议默认值 | 为什么必须确认 | 状态 |
| --- | --- | --- | --- | --- |
| `Q-001` | 规格包覆盖完整 P0-P5，还是先完整规格化当前阶段？ | 建立全局框架，先完成 P0-B 真实性审计和 P1 资产目录 | 决定文档深度、任务切片和验收成本 | `ACCEPTED` → `DEC-001` |
| `Q-002` | 保留现有实现逐项整改，还是允许推倒重做？ | 保留并以证据审计决定局部替换 | 决定迁移、兼容和历史数据策略 | `ACCEPTED` → `DEC-002` |
| `Q-003` | 是否接受 2026-07-02 快照所称“P0-B 已闭环”？ | 暂不接受，逐条重验 P0-B 出口 | 当前规范、契约和代码证据互相矛盾 | `ACCEPTED` → `DEC-003` |
| `Q-004` | 目标 AI IDE 是 Trae 专用还是多 IDE？ | 通用规格核心 + Trae 适配层 | 决定任务格式和 IDE 元数据位置 | `ACCEPTED` → `DEC-006` |
| `Q-005` | 规格评审完成前是否冻结产品代码？ | 冻结 P1+，只允许规格和证据审计 | 防止需求尚未稳定时继续扩大偏差 | `ACCEPTED` → `DEC-007` |
| `Q-006` | 首批必须跑通的用户旅程及优先级是什么？ | 登录/治理 → 数据集发现/详情 → 上传发布下载 → AI 搜索下载与受限贡献 | 用户旅程是纵向任务和 E2E 的边界 | `DEC-005` 已替代 → `DEC-008` |
| `Q-007` | “完成 P0”的精确边界是否严格采用 ADR-0002 的 P0-A/P0-B（公共底座及资产安全整改），资产登记/搜索/上传/发布仍按 P1-P3 顺序实施？ | 是；不改阶段定义，先让 P0-B 真实退出，再依次实现用户选择的非 Agent 旅程 | 用户确认 P0 未完成，同时又把资产到发布闭环列为首批目标，需明确是顺序目标还是改写 P0 | `OPEN` |

## 2. 第二轮：组织、主体与权限

| ID | 问题 | 建议默认值 | 影响 | 状态 |
| --- | --- | --- | --- | --- |
| `Q-101` | 是否为单公司租户、公司内多个组织和项目？ | 单租户、多组织、多项目 | 数据隔离、唯一约束和管理权限 | `ACCEPTED` → `DEC-004` |
| `Q-102` | 用户能否同时属于多个组织和项目？ | 可以 | PrincipalContext、列表过滤和角色绑定 | `OPEN` |
| `Q-103` | Team 是否是正式领域资源？ | 是；补齐 Team 表、API、成员和权限 | PRD 要求 Owner 可引用 Team，但当前模型缺失 | `OPEN` |
| `Q-104` | 资产 Owner 可以是个人、Team，还是必须为 Team？ | 至少一个 Team，可附加个人维护者 | 责任归属、离职交接和授权 | `OPEN` |
| `Q-105` | 谁可以创建组织、项目、Team 和资产？ | 平台管理员建组织；组织管理员建项目/Team；项目成员按角色建资产 | 权限矩阵和页面入口 | `OPEN` |
| `Q-106` | `PUBLIC` 是否仍只对已认证主体公开？ | 是 | 防匿名访问和搜索枚举 | `OPEN` |

## 3. 第三轮：资产、版本和发布

| ID | 问题 | 建议默认值 | 影响 | 状态 |
| --- | --- | --- | --- | --- |
| `Q-201` | 首个可交付版本必须支持 MODEL 和 DATASET 两类吗？ | 两类都支持，但共用主流程、分别定义字段 | 表单、验证、卡片和测试组合 | `OPEN` |
| `Q-202` | 资产名称、Namespace、重命名和旧别名的精确规则是什么？ | 组织内 Namespace 唯一；重命名保留永久跳转别名 | URL、Gitea 仓库和唯一约束 | `OPEN` |
| `Q-203` | 谁可以提交、审批和发布？提交人与审批人能否相同？ | 提交人与审批人分离；平台管理员仅紧急越权并审计 | 四眼原则和状态机 | `OPEN` |
| `Q-204` | 哪些 License、敏感等级或文件类型强制审批？ | 所有发布均审批；高敏感/高风险文件追加安全审核 | 策略、通知和审计 | `OPEN` |
| `Q-205` | 版本号是否强制 SemVer？数据集日期版本是否允许？ | 模型 SemVer；数据集允许受控日期版本 | 校验、排序和 API Schema | `OPEN` |
| `Q-206` | 软删除、归档和对象回收保留期分别多久？ | 元数据 90 天可恢复，正式对象需引用检查后回收 | 生命周期任务和恢复 | `OPEN` |

## 4. 第四轮：UI 与非功能目标

| ID | 问题 | 建议默认值 | 影响 | 状态 |
| --- | --- | --- | --- | --- |
| `Q-301` | 第一版是否必须同时支持 zh-CN/en-US 运行时切换？ | 是 | i18n 架构、文案和 E2E | `OPEN` |
| `Q-302` | 是否有现成 UI 品牌规范或参考产品？ | 若没有，以 Ant Design 企业后台为基线并先确认线框 | 页面规格和视觉验收 | `OPEN` |
| `Q-303` | MVP 用户数、并发、资产数、单文件和单会话规模是多少？ | 暂沿用 PRD：10 万资产、Web 20 GiB 上限 | 性能、索引和容量验收 | `OPEN` |
| `Q-304` | Compose 是唯一验收环境还是还需定义生产部署？ | Compose 为功能验收；生产部署另立规格和 ADR | Secret、HA、备份和 SLO | `OPEN` |
| `Q-305` | 浏览器 E2E 采用什么工具，是否要求截图/视觉回归？ | Playwright + 关键页面截图 | 页面“完成”的证据标准 | `OPEN` |

## 5. 决策记录格式

用户回答后，将问题转换为不可变决策记录：

```yaml
decisionId: DEC-...
questionId: Q-...
status: ACCEPTED
decision: ...
rationale: ...
decidedBy: ...
decidedAt: ...
affectedRequirements: [...]
affectedContracts: [...]
supersedes: null
```
