# 端到端用户旅程目录

> 状态：`PROPOSED`
> 说明：旅程定义“用户获得什么结果”，不是代码模块任务。

## 1. 推荐的首批验收顺序

依据 `DEC-001`、`DEC-003`、`DEC-008`，目标按阶段依赖顺序为：

1. `JRN-P0B-001` Bootstrap 管理员与安全登录；
2. `JRN-P0B-002` 用户登录、刷新、登出和禁用；
3. `JRN-P0B-003`、`JRN-P0B-004`、`JRN-P0B-005` 组织权限及字典/标签治理；
4. P0-B 其余公共底座旅程全部通过并真实退出；
5. `JRN-P1-001` 用户登记受治理资产；
6. `JRN-P1-002` 普通用户只发现有权资产；
7. `JRN-P2-002`、`JRN-P2-003` Web 上传和下载；
8. `JRN-P3-001` 校验、审批和发布闭环。
9. `JRN-P4-001`、`JRN-P4-002` AI 获取 JWT、搜索精确版本并下载；
10. `JRN-P4-005` 显式授权 AI 创建草稿并上传，最终进入人工审核。

`DEC-008` 替代了原“首批不含 Agent/MCP”的 `DEC-005`，但不是并行绕过阶段门禁的许可；
P1/P2/P3/P4 仍分别等待上游 VERIFIED。

## 2. P0-B 公共底座旅程

| ID | Actor | 起点 → 终点 | 必须覆盖 | 最低证据 |
| --- | --- | --- | --- | --- |
| `JRN-P0B-001` | 首个管理员 | Secret 引导 → 首次改密 → 正常访问管理端 | 一次性引导、Cookie 属性、访问 JWT 内存、审计、旧口令失效 | `E4` |
| `JRN-P0B-002` | 用户/管理员 | 登录 → 刷新轮换 → 登出/禁用 → 旧 Token 被拒 | 锁定、tokenVersion、重放、时钟边界、成功/失败审计 | `E4` |
| `JRN-P0B-003` | 平台/组织管理员 | 创建组织/项目/Team → 添加成员 → 绑定角色 | 越权隔离、自提权拒绝、停用作用域、审计 | `E4` |
| `JRN-P0B-004` | 授权管理员 | 定义角色 → 绑定 → ACL → 资源访问变化 | Scope 上限、实时授权、防枚举、数据库过滤 | `E4` |
| `JRN-P0B-005` | 治理管理员 | 创建字典/标签 → 业务引用 → 停用后历史回显 | zh/en、版本冲突、组织标签越权、自由值拒绝 | `E4` |
| `JRN-P0B-006` | 平台管理员 | 修改合法配置 → 热更新/重启提示 → 查询审计 | 类型校验、Secret 拒绝、乐观锁、二次确认 | `E4` |
| `JRN-P0B-007` | Worker/运维 | 创建可靠任务 → 失败重试 → Worker 重启 → 成功/DEAD | 租约、并发领取、退避、幂等、Dead 通知 | `E4` |
| `JRN-P0B-008` | 业务/通知接收者 | 核心事务提交 → 站内信/签名 Webhook → 失败后恢复 | Outbox 原子性、SSRF、签名、Delivery ID、重试 | `E4` |
| `JRN-P0B-009` | 审计员 | 触发成功/失败/拒绝 → 按条件查询审计 | 不可改删、脱敏、主体/资源/Trace 关联 | `E4` |
| `JRN-P0B-010` | 运维 | 请求触发后台任务 → 指标/Trace/日志/依赖诊断关联 | Prometheus Target、Span Link、诊断脱敏、告警 | `E4` |

## 3. P1 资产目录旅程

| ID | Actor | 起点 → 终点 | 必须覆盖 | 最低证据 |
| --- | --- | --- | --- | --- |
| `JRN-P1-001` | 资产维护者 | 选择组织/项目 → 填写受控元数据 → 创建 Gitea 仓库和草稿 | MODEL/DATASET、Owner、字典、tagId、幂等、建仓补偿 | `E4` |
| `JRN-P1-002` | 不同权限主体 | 搜索/筛选 → 只获得有权结果 → 查看详情 | PRIVATE/INTERNAL/PUBLIC、防枚举、Cursor、停用治理项回显 | `E4` |
| `JRN-P1-003` | Owner/维护者 | 更新卡片和治理字段 → Gitea/投影一致 | 乐观锁、外部失败、审计、历史别名 | `E4` |
| `JRN-P1-004` | Owner/管理员 | 删除/归档草稿资产 → 引用与保留策略生效 | 正式版本不可物理删、越权、幂等、恢复 | `E4` |
| `JRN-P1-005` | 获权用户 | 打开 Dataset Card → 参与 Discussion → 接收回复通知 | 资产权限继承、不可信内容、修订/撤回/Moderation、Outbox | `E4` |

## 4. P2 版本与数据面旅程

| ID | Actor | 起点 → 终点 | 必须覆盖 | 最低证据 |
| --- | --- | --- | --- | --- |
| `JRN-P2-001` | 研发用户 | Git/DVC 配置 → DVC push → 清理本地 → pull 校验 | 最小凭据、SHA-256、无永久 Secret 入库/日志 | `E4` |
| `JRN-P2-002` | Web 用户 | 创建上传会话 → Multipart → 刷新恢复 → Worker 转存 | Part 重试、限额、过期、路径安全、DVC 指针提交 | `E4` |
| `JRN-P2-003` | 获权用户/Agent | 请求下载票据 → 直连下载 → 校验 → 票据过期 | 状态/敏感度/用途限制、URL 脱敏、过期拒绝 | `E4` |
| `JRN-P2-004` | 数据使用者 | 数据集详情 → 选择精确版本 → Preview/Files → CLI pull/resume/verify | 同版本聚合、安全预览、路径过滤、摘要一致、无 Secret | `E4` |

## 5. P3 发布治理旅程

| ID | Actor | 起点 → 终点 | 必须覆盖 | 最低证据 |
| --- | --- | --- | --- | --- |
| `JRN-P3-001` | 维护者/审核者 | 校验 → 提交 → 审批/驳回 → 受保护 Tag 发布 | 四眼原则、状态机、Tag+Commit+Digest、通知/审计 | `E4` |
| `JRN-P3-002` | 故障注入者/Worker | 发布中 Gitea/MinIO 失败 → 重试/补偿 → 一致终态 | Saga、Outbox、幂等、无“DB 发布但无 Tag” | `E4` |
| `JRN-P3-003` | Owner/用户 | 弃用 → 搜索降权 → 获权下载 → 归档 | 已发布内容不变、原因字典、通知和引用检查 | `E4` |

## 6. P4 Agent 接入旅程

| ID | Actor | 起点 → 终点 | 必须覆盖 | 最低证据 |
| --- | --- | --- | --- | --- |
| `JRN-P4-001` | 只读 Agent 管理员 | 注册 Agent → Secret Store → 换 JWT → MCP initialize/list/call | 30 分钟接入、Tool 白名单+Scope、版本明确、无 Secret 泄漏 | `E4` |
| `JRN-P4-002` | 只读 Agent | 搜索 → 精确版本 → 下载票据 → 校验内容 | 小响应、Cursor、License/敏感度、URL 不进对话 | `E4` |
| `JRN-P4-003` | 越权 Agent | 调用隐藏/高风险 Tool → 被拒绝并审计 | tools/list 不泄露、403/Tool Error、AGENT_ACCESS_DENIED | `E4` |
| `JRN-P4-004` | OpenAPI Client | 导入裁剪契约 → 调用 Agent API → 下载 | 与 MCP 同一服务/权限/审计 | `E4` |
| `JRN-P4-005` | 显式授权写 Agent | 受控元数据创建草稿 → CLI 数据通道上传 → complete → 查询 Job → 提交人工审核 | 写 Tool 双控、幂等、无 URL/二进制进对话、禁止自动发布 | `E4` |

## 7. P5 质量与运维旅程

| ID | Actor | 起点 → 终点 | 必须覆盖 | 最低证据 |
| --- | --- | --- | --- | --- |
| `JRN-P5-001` | Reconciler/运维 | 模拟 Webhook 丢失或手工漂移 → 检测 → 修复投影 | Inbox、重复事件、审计、不可修复告警 | `E4` |
| `JRN-P5-002` | 备份管理员 | 备份 → 灾难清空 → 恢复 → 随机版本 DVC 校验 | PostgreSQL/Gitea/MinIO/Secret、RPO/RTO | `E5` |
| `JRN-P5-003` | 性能测试员 | 目标数据规模和并发 → 持续负载 → 报告 | P95/P99、错误率、资源、无权限泄漏 | `E5` |
| `JRN-P5-004` | 安全测试员 | 凭据/上传/SSRF/Prompt 注入攻击 → 拒绝和告警 | Secret 扫描、路径穿越、压缩炸弹、Card 不可信 | `E5` |

## 8. 旅程拆任务规则

- 一个旅程可拆多个任务，但最后必须由一个 E4/E5 场景重新串联；
- 不允许以“后端任务完成 + 前端任务完成”的数量替代旅程完成；
- 每个任务必须引用本目录中的 Journey ID、Requirement ID 和 Acceptance Scenario ID；
- 若旅程依赖未确认决策，只能编写规格，不能进入实现；
- 任何旅程的失败、越权、恢复分支不得被拆成无限期“后续优化”。
