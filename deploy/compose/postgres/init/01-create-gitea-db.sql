-- ============================================================================
-- 功能: 开发环境为 Gitea 创建独立数据库。生产环境应使用独立数据库账号与 Secret。
--       由 postgres 官方镜像的 docker-entrypoint-initdb.d 在首次初始化时执行。
-- 时间: 2026-06-29
-- 作者: AxeXie
-- ============================================================================

-- 为 Gitea 创建独立逻辑库，与平台业务库（POSTGRES_DB）隔离。
-- 开发环境复用 POSTGRES_USER 作为属主；生产环境应改为独立最小权限账号。
CREATE DATABASE gitea;
COMMENT ON DATABASE gitea IS 'Gitea 代码托管服务专用数据库（开发基线）';
